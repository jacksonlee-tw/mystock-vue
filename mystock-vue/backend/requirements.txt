fastapi>=0.110.0
uvicorn[standard]>=0.28.0
requests>=2.31.0
beautifulsoup4>=4.12  # 解析 TWSE/TPEx ISIN 代碼清單頁（services/symbol_master_fetcher.py），用內建 html.parser 不需 lxml
pandas>=2.2.0
lxml>=5.0
python-dotenv>=1.0.1
pydantic>=2.6.0
yfinance>=0.2.37
sqlalchemy>=2.0
asyncpg>=0.29
apscheduler>=3.10
PyYAML>=6.0
python-multipart>=0.0.9  # 個人投資記帳模組 CSV 交易匯入用檔案上傳（api/v1/endpoints/transactions.py）

# ── 整合訊息通知平台（backend/notify/，見系統開發規格書 §10）──
aiosmtplib>=3.0        # 非同步 SMTP（ADR-05）
httpx>=0.27            # Telegram Bot API（ADR-05）
jinja2>=3.1            # 模板渲染，SandboxedEnvironment（ADR-07）
cryptography>=42.0     # Fernet 加密管道憑證（ADR-08）
itsdangerous>=2.2      # 簽章 Cookie（ADR-10）
bcrypt>=4.1            # 擁有者密碼雜湊

# ── AI 技術分析報告（backend/ai/，見 docs/16.AI技術分析/AI技術分析規劃.md）──
anthropic>=0.40         # Claude API，AsyncAnthropic + messages.parse 結構化輸出（ADR-AI-04/06）
google-genai>=1.0       # Gemini API 現行統一 SDK（非舊版 google-generativeai，見規格書 D-02）

# ── 產業鏈知識圖譜與輪動模型（backend/indicators/lead_lag.py，見
#    docs/16.AI技術分析/Phase3-產業鏈知識圖譜與輪動模型.md §2.4）──
scipy>=1.11.0           # CCF 相關係數（scipy.stats.pearsonr）
statsmodels>=0.14.0     # FR-10 格蘭傑因果檢定（grangercausalitytests／multipletests）。ADR-IC-05
                        # 原先以「一次引入兩個統計套件卻只用得到其中一個」為由延後導入，該延後
                        # 已由使用者明確解除（見規格書 v2.7 修訂紀錄、新增 ADR-IC-22）
