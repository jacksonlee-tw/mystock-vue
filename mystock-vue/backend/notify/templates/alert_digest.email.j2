當日掃描完成，共觸發 {{ total_count }} 則訊號。

{% if sell_list %}■ 賣出訊號（{{ sell_list | length }}）
{% for a in sell_list %}  {{ a.stock_id }}　{{ a.stock_name }}　{{ a.strategy_name }}　{{ a.strength_label }}　{{ a.close | default('—') }}
{% endfor %}
{% endif -%}
{% if buy_list %}■ 買進訊號（{{ buy_list | length }}）
{% for a in buy_list %}  {{ a.stock_id }}　{{ a.stock_name }}　{{ a.strategy_name }}　{{ a.strength_label }}　{{ a.close | default('—') }}
{% endfor %}
{% endif -%}
{% if warning_list %}■ 警告訊號（{{ warning_list | length }}）
{% for a in warning_list %}  {{ a.stock_id }}　{{ a.stock_name }}　{{ a.strategy_name }}　{{ a.strength_label }}　{{ a.close | default('—') }}
{% endfor %}
{% endif -%}
▸ 開啟警示看板：{{ dashboard_url | default('') }}
{% if manage_url %}▸ 管理我的通知：{{ manage_url }}
{% endif %}

{{ disclaimer }}
